package Yash_Apps;

public class PrimitiveDataTypesDemo {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		
		boolean result = true;
		
		Boolean b = new Boolean(false);
		
		//heap memory ----> handler pool and Object Pool ----> memory will be located in object pool where false will be stored,
		// reference variable b will be stored in handler pool and it will point at memory in Object pool where false is stored.
		
		// Every Method has a stack memory where all its varaiable are stored and they are not accessible outside the method.

	}

}
